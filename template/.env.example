# 🎯 Docker Dev Environment Configuration
# This file will be copied to .env during installation with your user info auto-populated

# Docker Compose project name (must be unique per project)
COMPOSE_PROJECT_NAME=docker-dev-project

# User mapping (prevents permission issues between host and container)
HOST_UID=1000
HOST_GID=1000
HOST_USER=devuser

# Project configuration
PROJECT_NAME=project
PROJECT_PATH=/workspace

# Port mappings (customize per project to avoid conflicts)
# Set to empty to disable port exposure: PORT_BACKEND=
PORT_BACKEND=8000
PORT_FRONTEND=3000
PORT_VITE=5173
PORT_WEBSOCKET=8080

# Claude Code settings
CLAUDE_BYPASS_PERMISSIONS=true

# GitHub CLI authentication (optional)
# If gh auth on host uses macOS Keychain, set this to enable gh in container
# Create token at: https://github.com/settings/tokens
# Or run: gh auth token
# GH_TOKEN=ghp_your_token_here

# Optional: Add your project-specific environment variables below
# Example:
# DATABASE_URL=postgresql://localhost:5432/mydb
# API_KEY=your-api-key-here
